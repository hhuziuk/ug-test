{
  "name": "ug-test",
  "version": "1.0.0",
  "private": true,
  "packageManager": "pnpm@10.14.0",
  "scripts": {
    "build": "pnpm -r run build",
    "start": "pnpm -r run start:prod",
    "start:dev": "pnpm -r --parallel run start:dev",
    "lint": "biome check ./client/src ./server/src",
    "lint:fix": "biome format --write ./client/src ./server/src"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.1.3",
    "pnpm": "latest"
  }
}
